name: Deploy API Inventario na AWS

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    name: Deploy para a EC2
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Copiar arquivos para a AWS via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          # Ajustado para refletir o nome real da pasta backend-node e incluir a pasta dados
          source: "backend-node,frontend,dados,docker-compose.yml"
          target: "~/projeto-inventario"

      - name: Executar build e deploy via SSH na AWS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ~/projeto-inventario
            
            # Derruba os containers antigos
            docker compose down
            
            # Sobe os containers novamente recriando a imagem da API e do Frontend
            # O Docker lerá o docker-compose.yml que aponta para ./backend-node
            docker compose up -d --build
            
            # Limpa imagens antigas e órfãs para não lotar o disco de 30GB da AWS
            docker image prune -f