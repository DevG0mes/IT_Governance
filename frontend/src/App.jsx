import React, { useState, useEffect } from 'react';
import { ShieldCheck, Monitor, Users, X, LayoutDashboard, Database, Smartphone, Wifi, Laptop, Briefcase, Edit2, Save, Wrench, FileCheck, Clock, Cpu, Link as LinkIcon, MoreVertical, Info, LogOut, CheckCircle, Trash2, PowerOff, ListChecks, CreditCard, DollarSign } from 'lucide-react';

function App() {
  const [assets, setAssets] = useState([]);
  const [employees, setEmployees] = useState([]);
  const [licenses, setLicenses] = useState([]);
  
  const [isAssetModalOpen, setIsAssetModalOpen] = useState(false);
  const [isEmployeeModalOpen, setIsEmployeeModalOpen] = useState(false);
  const [isAssignEmployeeModalOpen, setIsAssignEmployeeModalOpen] = useState(false);
  const [isAssignAssetModalOpen, setIsAssignAssetModalOpen] = useState(false);
  const [isMaintenanceModalOpen, setIsMaintenanceModalOpen] = useState(false);
  const [isHistoryModalOpen, setIsHistoryModalOpen] = useState(false);
  const [isEditMaintenanceModalOpen, setIsEditMaintenanceModalOpen] = useState(false);
  const [isLicenseModalOpen, setIsLicenseModalOpen] = useState(false);
  
  const [editLicenseData, setEditLicenseData] = useState(null);
  const [viewLicenseUsers, setViewLicenseUsers] = useState(null);
  const [openActionMenu, setOpenActionMenu] = useState(null);
  const [editMaintenanceForm, setEditMaintenanceForm] = useState({ assetId: null, chamado: '', observacao: '' });
  const [viewAssetDetails, setViewAssetDetails] = useState(null); 
  const [isEditingDetails, setIsEditingDetails] = useState(false);
  const [editFormData, setEditFormData] = useState({});
  const [editEmployeeData, setEditEmployeeData] = useState(null);

  const [activeEmployee, setActiveEmployee] = useState(null);
  const [activeAsset, setActiveAsset] = useState(null);
  const [selectedItemForAssign, setSelectedItemForAssign] = useState('');
  const [maintenanceForm, setMaintenanceForm] = useState({ chamado: '', observacao: '' });

  const [activeTab, setActiveTab] = useState('inventory');
  const [selectedCategory, setSelectedCategory] = useState('Todos');

  const getInitialAsset = (type) => ({ asset_type: type === 'Todos' ? 'Notebook' : type, status: 'Disponível', serial_number: '', patrimonio: '', modelo_notebook: '', garantia: '', status_garantia: 'No prazo', modelo_starlink: '', projeto: '', localizacao: '', email: '', senha: '', senha_roteador: '', responsavel: '', imei: '', numero: '', iccid: '', modelo_celular: '' });
  const [newAsset, setNewAsset] = useState(getInitialAsset('Todos'));
  const [newEmployee, setNewEmployee] = useState({ nome: '', email: '', departamento: '' });
  const [newLicense, setNewLicense] = useState({ nome: '', fornecedor: '', plano: 'Mensal', custo: '', quantidade_total: '', data_renovacao: '' });

  const fetchData = () => {
    fetch('http://localhost:8080/api/assets').then(res => res.json()).then(data => setAssets(data.data || []));
    fetch('http://localhost:8080/api/employees').then(res => res.json()).then(data => setEmployees(data.data || []));
    fetch('http://localhost:8080/api/licenses').then(res => res.json()).then(data => setLicenses(data.data || []));
  };

  useEffect(() => { fetchData(); }, []);

  const getActiveAssetsForEmployee = (empId) => {
    const emp = employees.find(e => e.id === empId);
    if (!emp) return [];
    return assets.filter(a => {
        if (a.status !== 'Em uso') return false;
        const hasNewAssignment = a.assignments && a.assignments.some(asg => asg.employee_id === empId && !asg.returned_at);
        const hasLegacyAssignment = a.asset_type === 'Notebook' && a.notebook?.patrimonio === emp.notebook && emp.notebook !== "";
        return hasNewAssignment || hasLegacyAssignment;
    });
  };

  const handleCreateAsset = (e) => { 
    e.preventDefault(); 
    fetch('http://localhost:8080/api/assets', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({...newAsset, status: 'Disponível'}) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setIsAssetModalOpen(false); fetchData(); }).catch(err => alert("Erro: " + err.message)); 
  };
  
  const handleCreateEmployee = (e) => { 
    e.preventDefault(); 
    fetch('http://localhost:8080/api/employees', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(newEmployee) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setIsEmployeeModalOpen(false); setNewEmployee({ nome: '', email: '', departamento: '' }); fetchData(); }).catch(err => alert("Erro: " + err.message)); 
  };

  const submitAssignment = (e) => {
    e.preventDefault();
    const employeeId = activeEmployee ? activeEmployee.id : selectedItemForAssign;
    const assetId = activeAsset ? activeAsset.id : selectedItemForAssign;
    fetch(`http://localhost:8080/api/employees/${employeeId}/assign`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ asset_id: parseInt(assetId) }) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setIsAssignEmployeeModalOpen(false); setIsAssignAssetModalOpen(false); setSelectedItemForAssign(''); fetchData(); }).catch(err => alert("Erro: " + err.message));
  };

  const handleAction = (assetId, action) => {
    if (!window.confirm(`Tem certeza que deseja aplicar a ação: ${action.toUpperCase()}?`)) return;
    fetch(`http://localhost:8080/api/assets/${assetId}/${action}`, { method: 'PUT' })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setOpenActionMenu(null); fetchData(); }).catch(err => alert("Erro na ação: " + err.message));
  };

  const handleCreateLicense = (e) => {
    e.preventDefault();
    fetch('http://localhost:8080/api/licenses', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({...newLicense, custo: parseFloat(newLicense.custo), quantidade_total: parseInt(newLicense.quantidade_total)}) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setIsLicenseModalOpen(false); setNewLicense({ nome: '', fornecedor: '', plano: 'Mensal', custo: '', quantidade_total: '', data_renovacao: '' }); fetchData(); }).catch(err => alert("Erro: " + err.message));
  };

  const handleUpdateLicense = (e) => {
    e.preventDefault();
    fetch(`http://localhost:8080/api/licenses/${editLicenseData.id}`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({...editLicenseData, custo: parseFloat(editLicenseData.custo), quantidade_total: parseInt(editLicenseData.quantidade_total)}) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setEditLicenseData(null); fetchData(); }).catch(err => alert("Erro: " + err.message));
  };

  const assignLicenseToEmployee = (empId, licenseId) => {
    fetch('http://localhost:8080/api/licenses/assign', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ employee_id: empId, license_id: parseInt(licenseId) }) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { fetchData(); }).catch(err => alert("Erro: " + err.message));
  };

  const unassignLicense = (assignmentId) => {
    fetch(`http://localhost:8080/api/licenses/unassign/${assignmentId}`, { method: 'DELETE' }).then(() => fetchData());
  };

  const submitMaintenance = (e) => {
    e.preventDefault();
    fetch(`http://localhost:8080/api/assets/${activeAsset.id}/maintenance`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(maintenanceForm) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setIsMaintenanceModalOpen(false); setMaintenanceForm({chamado: '', observacao: ''}); fetchData(); }).catch(err => alert("Erro ao enviar: " + err.message));
  };

  const resolveMaintenance = (assetId) => {
    if (!window.confirm(`Deseja devolver este equipamento para o estoque como "Disponível"?`)) return;
    fetch(`http://localhost:8080/api/assets/${assetId}/resolve-maintenance`, { method: 'PUT' }).then(() => { setOpenActionMenu(null); fetchData(); });
  };

  const openEditMaintenance = (asset) => {
    const activeLog = asset.maintenance_logs?.find(log => !log.resolved_at);
    setEditMaintenanceForm({ assetId: asset.id, chamado: activeLog?.chamado || '', observacao: activeLog?.observacao || '' });
    setOpenActionMenu(null);
    setIsEditMaintenanceModalOpen(true);
  };

  const submitEditMaintenance = (e) => {
    e.preventDefault();
    fetch(`http://localhost:8080/api/assets/${editMaintenanceForm.assetId}/update-maintenance`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ chamado: editMaintenanceForm.chamado, observacao: editMaintenanceForm.observacao }) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setIsEditMaintenanceModalOpen(false); fetchData(); })
    .catch(err => alert("Erro ao atualizar: " + err.message));
  };

  const toggleEmployeeStatus = (empId) => {
    if(!window.confirm("Atenção: Desligar este colaborador devolverá TODOS os hardwares e LICENÇAS para o estoque. Continuar?")) return;
    fetch(`http://localhost:8080/api/employees/${empId}/toggle-status`, { method: 'PUT' })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setOpenActionMenu(null); setEditEmployeeData(null); fetchData(); }).catch(err => alert("Erro: " + err.message));
  };

  const handleDeleteEmployee = (empId) => {
    if (!window.confirm("🔴 EXCLUIR DEFINITIVAMENTE este colaborador? Esta ação não pode ser desfeita.")) return;
    fetch(`http://localhost:8080/api/employees/${empId}`, { method: 'DELETE' })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setOpenActionMenu(null); fetchData(); }).catch(err => alert("Erro: " + err.message));
  };

  const updateOffboardingData = (empId, field, value) => {
    const emp = employees.find(e => e.id === empId);
    const payload = { offboarding_onfly: field === 'onfly' ? value : emp.offboarding_onfly, offboarding_adm365: field === 'adm365' ? value : emp.offboarding_adm365, offboarding_license: field === 'license' ? value : emp.offboarding_license, termo_url: field === 'termo_url' ? value : (emp.termo_url || '') };
    fetch(`http://localhost:8080/api/employees/${empId}/offboarding`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) })
    .then(() => fetchData());
  };

  const saveEditEmployee = (e) => {
    e.preventDefault();
    fetch(`http://localhost:8080/api/employees/${editEmployeeData.id}`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(editEmployeeData) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setEditEmployeeData(null); fetchData(); }).catch(err => alert("Erro: " + err.message));
  };

  const openEditDetails = (asset) => {
    setEditFormData({
      asset_type: asset.asset_type, serial_number: asset.notebook?.serial_number || '', patrimonio: asset.notebook?.patrimonio || '', modelo_notebook: asset.notebook?.modelo || '', garantia: asset.notebook?.garantia || '', status_garantia: asset.notebook?.status_garantia || 'No prazo',
      modelo_starlink: asset.starlink?.modelo || '', projeto: asset.starlink?.projeto || '', localizacao: asset.starlink?.localizacao || '', responsavel: asset.starlink?.responsavel || '', email: asset.starlink?.email || '', senha: asset.starlink?.senha || '', senha_roteador: asset.starlink?.senha_roteador || '',
      imei: asset.celular?.imei || '', numero: asset.chip?.numero || '', iccid: asset.chip?.iccid || '', modelo_celular: asset.celular?.modelo || ''
    });
    setOpenActionMenu(null);
    setIsEditingDetails(true);
  };

  const saveEditDetails = (e) => { 
    e.preventDefault(); 
    fetch(`http://localhost:8080/api/assets/${viewAssetDetails.id}/details`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(editFormData) })
    .then(async res => { if(!res.ok) throw new Error((await res.json()).error); return res.json(); })
    .then(() => { setIsEditingDetails(false); setViewAssetDetails(null); fetchData(); })
    .catch(err => alert("Erro ao salvar detalhes: " + err.message));
  };

  const formatCurrency = (val) => new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(val);
  const activeEmployees = employees.filter(e => e.status !== 'Desligado');
  const offboardedEmployees = employees.filter(e => e.status === 'Desligado');
  const filteredAssets = selectedCategory === 'Todos' ? assets : assets.filter(a => a.asset_type === selectedCategory);
  const availableAssignables = assets.filter(a => a.status === 'Disponível' && ['Notebook', 'Celular', 'CHIP'].includes(a.asset_type));
  const maintenanceAssets = assets.filter(a => a.status === 'Manutenção');

  return (
    <div className="min-h-screen bg-brandDark font-sans selection:bg-brandGreen selection:text-white pb-32">
      <nav className="border-b border-gray-800 bg-black p-4"><div className="max-w-7xl mx-auto flex justify-between items-center"><div className="flex items-center gap-2"><ShieldCheck className="text-brandGreen w-8 h-8" /><span className="text-xl font-bold tracking-wider text-white">PSI Energy <span className="text-brandGreen">GovTI</span></span></div><div className="flex gap-6 overflow-x-auto no-scrollbar"><button onClick={() => setActiveTab('dashboard')} className={`text-sm font-semibold flex items-center gap-2 ${activeTab === 'dashboard' ? 'text-brandGreen' : 'text-gray-400 hover:text-white'}`}><LayoutDashboard className="w-4 h-4"/> Dashboard</button><button onClick={() => setActiveTab('inventory')} className={`text-sm font-semibold flex items-center gap-2 ${activeTab === 'inventory' ? 'text-brandGreen' : 'text-gray-400 hover:text-white'}`}><Database className="w-4 h-4"/> Inventário</button><button onClick={() => setActiveTab('licenses')} className={`text-sm font-semibold flex items-center gap-2 ${activeTab === 'licenses' ? 'text-brandGreen' : 'text-gray-400 hover:text-white'}`}><CreditCard className="w-4 h-4"/> Licenças</button><button onClick={() => setActiveTab('employees')} className={`text-sm font-semibold flex items-center gap-2 ${activeTab === 'employees' ? 'text-brandGreen' : 'text-gray-400 hover:text-white'}`}><Users className="w-4 h-4"/> Colaboradores</button><button onClick={() => setActiveTab('maintenance')} className={`text-sm font-semibold flex items-center gap-2 ${activeTab === 'maintenance' ? 'text-yellow-400' : 'text-gray-400 hover:text-white'}`}><Wrench className="w-4 h-4"/> Manutenção</button><button onClick={() => setActiveTab('offboarding')} className={`text-sm font-semibold flex items-center gap-2 ${activeTab === 'offboarding' ? 'text-red-400' : 'text-gray-400 hover:text-white'}`}><FileCheck className="w-4 h-4"/> Revogação</button></div></div></nav>

      <main className="max-w-7xl mx-auto p-6 mt-6">
        {activeTab === 'dashboard' && (<div className="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8"><div className="bg-gray-900 border border-gray-800 p-6 rounded-3xl"><div className="flex items-center gap-4 mb-4"><div className="p-3 bg-gray-800 rounded-full text-brandGreen"><Monitor className="w-6 h-6" /></div><h2 className="text-sm font-semibold text-gray-200">Total de Hardwares</h2></div><p className="text-3xl font-bold text-white">{assets.length}</p></div><div className="bg-gray-900 border border-gray-800 p-6 rounded-3xl"><div className="flex items-center gap-4 mb-4"><div className="p-3 bg-gray-800 rounded-full text-blue-400"><CreditCard className="w-6 h-6" /></div><h2 className="text-sm font-semibold text-gray-200">Licenças Ativas</h2></div><p className="text-3xl font-bold text-white">{licenses.reduce((acc, curr) => acc + curr.quantidade_em_uso, 0)}</p></div><div className="bg-gray-900 border border-gray-800 p-6 rounded-3xl"><div className="flex items-center gap-4 mb-4"><div className="p-3 bg-gray-800 rounded-full text-yellow-400"><Wrench className="w-6 h-6" /></div><h2 className="text-sm font-semibold text-gray-200">Em Manutenção</h2></div><p className="text-3xl font-bold text-white">{maintenanceAssets.length}</p></div><div className="bg-gray-900 border border-gray-800 p-6 rounded-3xl"><div className="flex items-center gap-4 mb-4"><div className="p-3 bg-gray-800 rounded-full text-red-400"><DollarSign className="w-6 h-6" /></div><h2 className="text-sm font-semibold text-gray-200">Custo Total (Mês)</h2></div><p className="text-3xl font-bold text-white">{formatCurrency(licenses.filter(l => l.plano === 'Mensal').reduce((acc, l) => acc + (l.custo * l.quantidade_total), 0))}</p></div></div>)}
        
        {activeTab === 'licenses' && (<div><div className="flex justify-between items-center mb-6"><div><h2 className="text-2xl font-bold text-white flex items-center gap-2"><CreditCard className="text-brandGreen"/> Gestão de FinOps & Software</h2></div><button onClick={() => setIsLicenseModalOpen(true)} className="bg-brandGreen hover:bg-brandGreenHover text-white px-6 py-2.5 rounded-full font-semibold shadow-[0_0_15px_rgba(16,185,129,0.3)]">+ Nova Licença</button></div><div className="bg-gray-900 border border-gray-800 rounded-3xl shadow-xl min-h-[400px]"><table className="w-full text-left text-sm text-gray-300"><thead className="bg-black/50 text-gray-400 border-b border-gray-800 uppercase text-xs font-semibold"><tr><th className="px-6 py-4 rounded-tl-3xl">Software / Plano</th><th className="px-6 py-4">Custo Unitário</th><th className="px-6 py-4">Estoque (Uso / Total)</th><th className="px-6 py-4">Custo Fixo Total</th><th className="px-6 py-4 text-center rounded-tr-3xl">Ações</th></tr></thead><tbody className="divide-y divide-gray-800">{licenses.map(lic => { const isEstourando = lic.quantidade_em_uso >= lic.quantidade_total; return (<tr key={lic.id} className="hover:bg-gray-800/50"><td className="px-6 py-4 font-bold text-white"><p>{lic.nome}</p><p className="text-xs text-gray-500 font-normal">{lic.fornecedor} • Cobrança: <span className={lic.plano === 'Mensal' ? 'text-blue-400' : 'text-purple-400'}>{lic.plano}</span> {lic.data_renovacao && `• Renova em: ${new Date(lic.data_renovacao + 'T00:00:00').toLocaleDateString('pt-BR')}`}</p></td><td className="px-6 py-4">{formatCurrency(lic.custo)}</td><td className="px-6 py-4"><div className="flex items-center gap-2"><span className={`px-2 py-1 rounded text-xs font-bold ${isEstourando ? 'bg-red-900/30 text-red-400' : 'bg-green-900/30 text-brandGreen'}`}>{lic.quantidade_em_uso} / {lic.quantidade_total}</span>{isEstourando && <span className="text-[10px] text-red-500 uppercase">Esgotado</span>}</div></td><td className="px-6 py-4 font-mono text-gray-200">{formatCurrency(lic.custo * lic.quantidade_total)} / {lic.plano.substring(0,3).toLowerCase()}.</td><td className="px-6 py-4 relative text-center"><button onClick={() => setOpenActionMenu(openActionMenu === `lic-${lic.id}` ? null : `lic-${lic.id}`)} className="p-2 bg-gray-800 hover:bg-gray-700 rounded-lg text-gray-300 hover:text-white"><MoreVertical className="w-5 h-5" /></button>{openActionMenu === `lic-${lic.id}` && (<><div className="fixed inset-0 z-30" onClick={() => setOpenActionMenu(null)}></div><div className="absolute right-8 top-10 w-56 bg-gray-800 border border-gray-700 rounded-xl shadow-2xl z-40 overflow-hidden py-2 text-left"><button onClick={() => { setOpenActionMenu(null); setViewLicenseUsers(lic); }} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><Users className="w-4 h-4 text-blue-400"/> Ver Usuários</button><div className="border-t border-gray-700 my-1"></div><button onClick={() => { setOpenActionMenu(null); setEditLicenseData(lic); }} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><Edit2 className="w-4 h-4 text-yellow-400"/> Editar Plano</button></div></>)}</td></tr>)})}</tbody></table></div></div>)}

        {activeTab === 'inventory' && (<div className="flex gap-8 items-start"><div className="w-64 flex-shrink-0 flex flex-col gap-2"><button onClick={() => { setNewAsset(getInitialAsset(selectedCategory)); setIsAssetModalOpen(true); }} className="mb-4 bg-brandGreen hover:bg-brandGreenHover text-white px-6 py-3 rounded-2xl font-semibold shadow-[0_0_15px_rgba(16,185,129,0.3)] w-full text-center">+ Novo Ativo</button>{[{ name: 'Todos', icon: <Database className="w-5 h-5" /> }, { name: 'Notebook', icon: <Laptop className="w-5 h-5" /> }, { name: 'Celular', icon: <Smartphone className="w-5 h-5" /> }, { name: 'Starlink', icon: <Wifi className="w-5 h-5" /> }, { name: 'CHIP', icon: <Cpu className="w-5 h-5" /> }].map(cat => (<button key={cat.name} onClick={() => setSelectedCategory(cat.name)} className={`flex items-center gap-3 px-4 py-3 rounded-2xl text-left font-medium transition-all ${selectedCategory === cat.name ? 'bg-gray-800 text-brandGreen border border-gray-700' : 'text-gray-400 hover:bg-gray-900 hover:text-white border border-transparent'}`}>{cat.icon} {cat.name}</button>))}</div><div className="flex-1 bg-gray-900 border border-gray-800 rounded-3xl shadow-xl min-h-[400px]"><table className="w-full text-left text-sm text-gray-300"><thead className="bg-black/50 text-gray-400 border-b border-gray-800 uppercase text-xs font-semibold"><tr><th className="px-6 py-4 rounded-tl-3xl">Equipamento</th><th className="px-6 py-4">Status</th><th className="px-6 py-4">Identificador Principal</th><th className="px-6 py-4 text-center rounded-tr-3xl">Ações</th></tr></thead><tbody className="divide-y divide-gray-800">{filteredAssets.map(asset => { const activeAsg = asset.assignments?.find(a => !a.returned_at); const ownerName = activeAsg ? activeAsg.employee?.nome : null; return (<tr key={asset.id} className="hover:bg-gray-800/50"><td className="px-6 py-4 font-bold text-white flex items-center gap-2">{asset.asset_type === 'Starlink' && <Wifi className="w-4 h-4 text-gray-400" />}{asset.asset_type === 'Notebook' && <Laptop className="w-4 h-4 text-gray-400" />}{asset.asset_type === 'Celular' && <Smartphone className="w-4 h-4 text-gray-400" />}{asset.asset_type === 'CHIP' && <Cpu className="w-4 h-4 text-gray-400" />}{asset.asset_type}</td><td className="px-6 py-4"><span className={`whitespace-nowrap inline-block px-3 py-1 border rounded-full text-xs font-medium ${asset.status === 'Disponível' ? 'bg-green-900/30 text-brandGreen border-brandGreen/20' : asset.status === 'Em uso' ? 'bg-blue-900/30 text-blue-400 border-blue-400/20' : 'bg-yellow-900/30 text-yellow-400 border-yellow-400/20'}`}>{asset.status}</span></td><td className="px-6 py-4">{asset.asset_type === 'Notebook' && asset.notebook && (<div><p className="font-semibold text-gray-200">Patrimônio: {asset.notebook.patrimonio}</p><p className="text-xs text-gray-500">Mod: {asset.notebook.modelo}</p></div>)}{asset.asset_type === 'Starlink' && asset.starlink && (<div><p className="font-semibold text-gray-200">Projeto: {asset.starlink.projeto}</p><p className="text-xs text-gray-500">Local: {asset.starlink.localizacao}</p></div>)}{asset.asset_type === 'Celular' && asset.celular && (<div><p className="font-semibold text-gray-200">IMEI: {asset.celular.imei}</p><p className="text-xs text-gray-500">Mod: {asset.celular.modelo}</p></div>)}{asset.asset_type === 'CHIP' && asset.chip && (<div><p className="font-semibold text-gray-200">Linha: {asset.chip.numero}</p><p className="text-xs text-gray-500">ICCID: {asset.chip.iccid}</p></div>)}{asset.asset_type !== 'Starlink' && asset.status === 'Em uso' && ownerName && (<p className="text-xs text-brandGreen font-semibold mt-1 flex items-center gap-1"><Users className="w-3 h-3"/> {ownerName}</p>)}{asset.asset_type === 'Starlink' && asset.starlink?.responsavel && (<p className="text-xs text-brandGreen font-semibold mt-1 flex items-center gap-1"><Users className="w-3 h-3"/> {asset.starlink.responsavel}</p>)}</td><td className="px-6 py-4 relative text-center"><button onClick={() => setOpenActionMenu(openActionMenu === `asset-${asset.id}` ? null : `asset-${asset.id}`)} className="p-2 bg-gray-800 hover:bg-gray-700 rounded-lg text-gray-300 hover:text-white"><MoreVertical className="w-5 h-5" /></button>{openActionMenu === `asset-${asset.id}` && (<><div className="fixed inset-0 z-30" onClick={() => setOpenActionMenu(null)}></div><div className="absolute right-8 top-10 w-56 bg-gray-800 border border-gray-700 rounded-xl shadow-2xl z-40 overflow-hidden py-2 text-left"><button onClick={() => { setOpenActionMenu(null); setViewAssetDetails(asset); setIsEditingDetails(false); }} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><Info className="w-4 h-4 text-blue-400"/> Detalhes / Editar</button><button onClick={() => { setOpenActionMenu(null); setActiveAsset(asset); setIsHistoryModalOpen(true); }} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><Clock className="w-4 h-4 text-purple-400"/> Ver Histórico</button><div className="border-t border-gray-700 my-1"></div>{asset.status === 'Disponível' && asset.asset_type !== 'Starlink' && <button onClick={() => { setOpenActionMenu(null); setActiveAsset(asset); setIsAssignAssetModalOpen(true); setSelectedItemForAssign(''); }} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><CheckCircle className="w-4 h-4 text-brandGreen"/> Atribuir a Alguém</button>}{asset.status === 'Em uso' && <button onClick={() => handleAction(asset.id, 'unassign')} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><LogOut className="w-4 h-4 text-red-400"/> Remover Atribuição</button>}{asset.status !== 'Manutenção' && <button onClick={() => { setOpenActionMenu(null); setActiveAsset(asset); setIsMaintenanceModalOpen(true); }} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><Wrench className="w-4 h-4 text-yellow-400"/> Enviar p/ Manutenção</button>}{asset.status === 'Manutenção' && <button onClick={() => handleAction(asset.id, 'unassign')} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><CheckCircle className="w-4 h-4 text-brandGreen"/> Disponibilizar Eqp.</button>}</div></>)}</td></tr>)})}</tbody></table></div></div>)}

        {activeTab === 'employees' && (<div><div className="flex justify-end mb-4"><button onClick={() => setIsEmployeeModalOpen(true)} className="bg-brandGreen hover:bg-brandGreenHover text-white px-6 py-2 rounded-full font-semibold shadow-[0_0_15px_rgba(16,185,129,0.3)]">+ Novo Colaborador</button></div><div className="bg-gray-900 border border-gray-800 rounded-3xl shadow-xl min-h-[400px]"><table className="w-full text-left text-sm text-gray-300"><thead className="bg-black/50 text-gray-400 border-b border-gray-800 uppercase text-xs font-semibold"><tr><th className="px-6 py-4 rounded-tl-3xl">Nome</th><th className="px-6 py-4">Departamento</th><th className="px-6 py-4">Ativos Vinculados</th><th className="px-6 py-4 text-center rounded-tr-3xl">Ações</th></tr></thead><tbody className="divide-y divide-gray-800">{activeEmployees.map(emp => { const empAssets = getActiveAssetsForEmployee(emp.id); const empLicenses = emp.assigned_licenses || []; return (<tr key={emp.id} className="hover:bg-gray-800/50"><td className="px-6 py-4 font-bold text-white flex items-center gap-3"><div className="w-8 h-8 rounded-full flex items-center justify-center font-bold bg-brandGreen/20 text-brandGreen">{emp.nome.charAt(0).toUpperCase()}</div>{emp.nome}</td><td className="px-6 py-4 text-gray-400">{emp.departamento}</td><td className="px-6 py-4"><div className="flex flex-wrap gap-2">{empAssets.some(a => a.asset_type === 'Notebook') && <span className="bg-blue-900/20 text-blue-400 px-2 py-1 rounded text-xs border border-blue-900/50">Notebook</span>}{empAssets.some(a => a.asset_type === 'Celular') && <span className="bg-purple-900/20 text-purple-400 px-2 py-1 rounded text-xs border border-purple-900/50">Celular</span>}{empLicenses.length > 0 && <span className="bg-yellow-900/20 text-yellow-400 px-2 py-1 rounded text-xs border border-yellow-900/50">{empLicenses.length} Licença(s)</span>}{empAssets.length === 0 && empLicenses.length === 0 && <span className="text-gray-500">Nenhum</span>}</div></td><td className="px-6 py-4 relative text-center"><button onClick={() => setOpenActionMenu(openActionMenu === `emp-${emp.id}` ? null : `emp-${emp.id}`)} className="p-2 bg-gray-800 hover:bg-gray-700 rounded-lg text-gray-300 hover:text-white"><MoreVertical className="w-5 h-5" /></button>{openActionMenu === `emp-${emp.id}` && (<><div className="fixed inset-0 z-30" onClick={() => setOpenActionMenu(null)}></div><div className="absolute right-8 top-10 w-56 bg-gray-800 border border-gray-700 rounded-xl shadow-2xl z-40 overflow-hidden py-2 text-left"><button onClick={() => { setOpenActionMenu(null); setEditEmployeeData(emp); }} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><ListChecks className="w-4 h-4 text-blue-400"/> Gerenciar Perfil</button><div className="border-t border-gray-700 my-1"></div><button onClick={() => { setOpenActionMenu(null); setActiveEmployee(emp); setIsAssignEmployeeModalOpen(true); setSelectedItemForAssign(''); }} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><CheckCircle className="w-4 h-4 text-brandGreen"/> Atribuir Novo Eqp.</button><div className="border-t border-gray-700 my-1"></div><button onClick={() => toggleEmployeeStatus(emp.id)} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><PowerOff className="w-4 h-4 text-yellow-500"/> Iniciar Desligamento</button><button onClick={() => handleDeleteEmployee(emp.id)} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><Trash2 className="w-4 h-4 text-red-500"/> Excluir</button></div></>)}</td></tr>)})}</tbody></table></div></div>)}
        
        {activeTab === 'maintenance' && (<div className="bg-gray-900 border border-gray-800 rounded-3xl shadow-xl min-h-[400px]"><div className="p-6 border-b border-gray-800 flex justify-between items-center"><h2 className="text-xl font-bold text-white flex items-center gap-2"><Wrench className="text-yellow-400"/> Equipamentos em Manutenção</h2></div><table className="w-full text-left text-sm text-gray-300"><thead className="bg-black/50 text-gray-400 border-b border-gray-800 uppercase text-xs font-semibold"><tr><th className="px-6 py-4">Equipamento</th><th className="px-6 py-4">Nº Chamado</th><th className="px-6 py-4">Observação / Tratativa</th><th className="px-6 py-4 text-center">Ações</th></tr></thead><tbody className="divide-y divide-gray-800">{maintenanceAssets.length === 0 ? (<tr><td colSpan="4" className="text-center py-8 text-gray-500">Nenhum equipamento em manutenção no momento.</td></tr>) : maintenanceAssets.map(asset => { const activeLog = asset.maintenance_logs?.find(log => !log.resolved_at); return (<tr key={asset.id} className="hover:bg-gray-800/50"><td className="px-6 py-4 font-bold text-white">{asset.asset_type}</td><td className="px-6 py-4 text-yellow-400 font-mono">{activeLog?.chamado || '-'}</td><td className="px-6 py-4 text-gray-400 italic max-w-xs truncate">{activeLog?.observacao || 'Sem observações'}</td><td className="px-6 py-4 relative text-center"><button onClick={() => setOpenActionMenu(openActionMenu === `maint-${asset.id}` ? null : `maint-${asset.id}`)} className="p-2 bg-gray-800 hover:bg-gray-700 rounded-lg text-gray-300 hover:text-white"><MoreVertical className="w-5 h-5" /></button>{openActionMenu === `maint-${asset.id}` && (<><div className="fixed inset-0 z-30" onClick={() => setOpenActionMenu(null)}></div><div className="absolute right-8 top-10 w-48 bg-gray-800 border border-gray-700 rounded-xl shadow-2xl z-40 overflow-hidden py-2 text-left"><button onClick={() => openEditMaintenance(asset)} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><Edit2 className="w-4 h-4 text-blue-400"/> Atualizar</button><div className="border-t border-gray-700 my-1"></div><button onClick={() => resolveMaintenance(asset.id)} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><CheckCircle className="w-4 h-4 text-brandGreen"/> Finalizar</button></div></>)}</td></tr>)})}</tbody></table></div>)}

        {activeTab === 'offboarding' && (<div className="bg-gray-900 border border-gray-800 rounded-3xl shadow-xl"><div className="p-6 border-b border-gray-800"><h2 className="text-xl font-bold text-white flex items-center gap-2"><FileCheck className="text-red-400"/> Checklist de Desligamentos (Offboarding)</h2></div><table className="w-full text-left text-sm text-gray-300"><thead className="bg-black/50 text-gray-400 border-b border-gray-800 uppercase text-xs font-semibold"><tr><th className="px-6 py-4">Colaborador</th><th className="px-6 py-4">Desligamento</th><th className="px-6 py-4 text-center">Acessos</th><th className="px-6 py-4 text-center">Ações</th></tr></thead><tbody className="divide-y divide-gray-800">{offboardedEmployees.length === 0 ? (<tr><td colSpan="4" className="text-center py-8 text-gray-500">Nenhum colaborador desligado.</td></tr>) : offboardedEmployees.map(emp => (<tr key={emp.id} className="hover:bg-gray-800/50"><td className="px-6 py-4 font-bold text-white"><div className="flex items-center gap-3"><div className="w-8 h-8 rounded-full flex items-center justify-center font-bold bg-gray-800 text-gray-500">{emp.nome.charAt(0).toUpperCase()}</div>{emp.nome}</div></td><td className="px-6 py-4 text-gray-400">{emp.offboarding_date ? new Date(emp.offboarding_date).toLocaleDateString('pt-BR') : ''}</td><td className="px-6 py-4"><div className="flex flex-col gap-2 justify-center ml-4"><label className="flex items-center gap-2 cursor-pointer hover:text-white"><input type="checkbox" checked={emp.offboarding_onfly} onChange={(e) => updateOffboardingData(emp.id, 'onfly', e.target.checked)} className="w-4 h-4 accent-brandGreen"/> Onfly</label><label className="flex items-center gap-2 cursor-pointer hover:text-white"><input type="checkbox" checked={emp.offboarding_adm365} onChange={(e) => updateOffboardingData(emp.id, 'adm365', e.target.checked)} className="w-4 h-4 accent-brandGreen"/> ADM 365</label></div></td><td className="px-6 py-4 relative text-center"><button onClick={() => setOpenActionMenu(openActionMenu === `off-${emp.id}` ? null : `off-${emp.id}`)} className="p-2 bg-gray-800 hover:bg-gray-700 rounded-lg text-gray-300 hover:text-white"><MoreVertical className="w-5 h-5" /></button>{openActionMenu === `off-${emp.id}` && (<><div className="fixed inset-0 z-30" onClick={() => setOpenActionMenu(null)}></div><div className="absolute right-8 top-10 w-56 bg-gray-800 border border-gray-700 rounded-xl shadow-2xl z-40 overflow-hidden py-2 text-left"><button onClick={() => toggleEmployeeStatus(emp.id)} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><CheckCircle className="w-4 h-4 text-brandGreen"/> Reativar Cadastro</button><div className="border-t border-gray-700 my-1"></div><button onClick={() => handleDeleteEmployee(emp.id)} className="w-full px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white flex items-center gap-3"><Trash2 className="w-4 h-4 text-red-500"/> Excluir Definitivamente</button></div></>)}</td></tr>))}</tbody></table></div>)}
      </main>

      {/* JANELAS MODAIS */}

      {viewLicenseUsers && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-2xl shadow-2xl relative"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white flex items-center gap-2"><Users className="text-blue-400"/> Usuários da Licença: {viewLicenseUsers.nome}</h2><button onClick={() => setViewLicenseUsers(null)} className="text-gray-400 hover:text-white p-2"><X className="w-6 h-6" /></button></div><div className="max-h-96 overflow-y-auto pr-2">{(!viewLicenseUsers.assignments || viewLicenseUsers.assignments.length === 0) ? (<p className="text-gray-500 text-center py-4">Nenhum colaborador está utilizando esta licença no momento.</p>) : (<div className="space-y-3">{viewLicenseUsers.assignments.map((asg) => (<div key={asg.id} className="bg-black border border-gray-800 p-4 rounded-xl flex justify-between items-center"><div><p className="text-brandGreen font-bold">{asg.employee?.nome || 'Desconhecido'}</p><p className="text-xs text-gray-500 mt-1">{asg.employee?.departamento || 'Sem departamento'}</p></div><button onClick={() => { unassignLicense(asg.id); setViewLicenseUsers(null); }} className="text-xs font-semibold text-red-400 hover:text-white transition-colors bg-gray-900 px-3 py-2 rounded border border-gray-800">Revogar Acesso</button></div>))}</div>)}</div></div></div>)}
      
      {editEmployeeData && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50 overflow-y-auto"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-5xl shadow-2xl my-8 relative"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-2xl font-bold text-white flex items-center gap-2"><Users className="text-brandGreen"/> Painel: {editEmployeeData.nome}</h2><button onClick={() => setEditEmployeeData(null)} className="text-gray-400 hover:text-brandGreen"><X className="w-6 h-6" /></button></div><div className="grid grid-cols-1 md:grid-cols-3 gap-8"><form onSubmit={saveEditEmployee} className="flex flex-col gap-4 col-span-1 border-r border-gray-800 pr-6"><h3 className="text-gray-300 font-bold mb-2">Dados Cadastrais</h3><input type="text" placeholder="Nome" required value={editEmployeeData.nome} onChange={(e) => setEditEmployeeData({...editEmployeeData, nome: e.target.value})} className="bg-black border border-gray-700 rounded-lg p-3 text-white focus:border-brandGreen outline-none" /><input type="email" placeholder="E-mail" required value={editEmployeeData.email} onChange={(e) => setEditEmployeeData({...editEmployeeData, email: e.target.value})} className="bg-black border border-gray-700 rounded-lg p-3 text-white focus:border-brandGreen outline-none" /><input type="text" placeholder="Departamento" required value={editEmployeeData.departamento} onChange={(e) => setEditEmployeeData({...editEmployeeData, departamento: e.target.value})} className="bg-black border border-gray-700 rounded-lg p-3 text-white focus:border-brandGreen outline-none" /><button type="submit" className="w-full bg-gray-800 hover:bg-gray-700 text-white py-3 rounded-xl font-bold mt-2 shadow-lg transition-colors border border-gray-700">Salvar Alterações</button></form><div className="col-span-1 border-r border-gray-800 pr-6"><h3 className="text-brandGreen font-bold mb-4 flex items-center gap-2"><Laptop className="w-4 h-4"/> Hardware Físico</h3><div className="mb-4"><select onChange={(e) => { if(e.target.value) { submitAssignment({preventDefault: () => {}}, editEmployeeData.id, e.target.value); e.target.value=''; } }} className="w-full bg-black border border-brandGreen/50 rounded-lg p-2 text-sm text-brandGreen outline-none"><option value="">+ Atribuir Hardware do Estoque</option>{assets.filter(a => a.status === 'Disponível' && ['Notebook', 'Celular', 'CHIP'].includes(a.asset_type)).map(a => (<option key={a.id} value={a.id}>{a.asset_type}: {a.notebook?.patrimonio || a.celular?.imei || a.chip?.numero}</option>))}</select></div><div className="space-y-3 max-h-64 overflow-y-auto pr-2">{getActiveAssetsForEmployee(editEmployeeData.id).map(asset => (<div key={asset.id} className="bg-black p-3 rounded-lg flex justify-between items-center border border-gray-700"><div><p className="text-white font-bold text-sm">{asset.asset_type}</p><p className="text-xs text-gray-400">{asset.notebook?.patrimonio || asset.celular?.imei || asset.chip?.numero}</p></div><button onClick={() => { handleAction(asset.id, 'unassign'); setEditEmployeeData(null); }} className="text-xs font-semibold text-red-400 hover:text-white transition-colors bg-red-900/30 px-3 py-2 rounded border border-red-900/50">Devolver</button></div>))}</div></div><div className="col-span-1"><h3 className="text-blue-400 font-bold mb-4 flex items-center gap-2"><CreditCard className="w-4 h-4"/> Licenças em Uso</h3><div className="mb-4"><select onChange={(e) => { if(e.target.value) { assignLicenseToEmployee(editEmployeeData.id, e.target.value); e.target.value=''; setEditEmployeeData(null); } }} className="w-full bg-black border border-blue-900/50 rounded-lg p-2 text-sm text-blue-400 outline-none"><option value="">+ Adicionar Licença</option>{licenses.filter(l => l.quantidade_em_uso < l.quantidade_total).map(l => (<option key={l.id} value={l.id}>{l.nome} ({l.quantidade_total - l.quantidade_em_uso} livres)</option>))}</select></div><div className="space-y-3 max-h-64 overflow-y-auto pr-2">{(editEmployeeData.assigned_licenses || []).map(asg => (<div key={asg.id} className="bg-black p-3 rounded-lg flex justify-between items-center border border-gray-700"><div><p className="text-white font-bold text-sm">{asg.license?.nome}</p><p className="text-[10px] text-gray-500 uppercase">{asg.license?.plano}</p></div><button onClick={() => { unassignLicense(asg.id); setEditEmployeeData(null); }} className="text-xs font-semibold text-red-400 hover:text-white transition-colors bg-gray-900 px-3 py-2 rounded border border-gray-800">Revogar</button></div>))}</div></div></div></div></div>)}

      {(isLicenseModalOpen || editLicenseData) && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-md shadow-2xl"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white flex items-center gap-2"><CreditCard className="w-5 h-5 text-brandGreen"/> {editLicenseData ? 'Editar Licença' : 'Nova Licença'}</h2><button onClick={() => {setIsLicenseModalOpen(false); setEditLicenseData(null);}} className="text-gray-400 hover:text-brandGreen"><X className="w-6 h-6" /></button></div><form onSubmit={editLicenseData ? handleUpdateLicense : handleCreateLicense} className="flex flex-col gap-4"><div><label className="block text-xs font-medium text-gray-400 mb-1">Nome do Software</label><input type="text" required value={editLicenseData ? editLicenseData.nome : newLicense.nome} onChange={(e) => editLicenseData ? setEditLicenseData({...editLicenseData, nome: e.target.value}) : setNewLicense({...newLicense, nome: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white focus:border-brandGreen outline-none" /></div><div className="grid grid-cols-2 gap-4"><div><label className="block text-xs font-medium text-gray-400 mb-1">Fornecedor</label><input type="text" value={editLicenseData ? editLicenseData.fornecedor : newLicense.fornecedor} onChange={(e) => editLicenseData ? setEditLicenseData({...editLicenseData, fornecedor: e.target.value}) : setNewLicense({...newLicense, fornecedor: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white focus:border-brandGreen outline-none" /></div><div><label className="block text-xs font-medium text-gray-400 mb-1">Frequência</label><select value={editLicenseData ? editLicenseData.plano : newLicense.plano} onChange={(e) => editLicenseData ? setEditLicenseData({...editLicenseData, plano: e.target.value}) : setNewLicense({...newLicense, plano: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white outline-none"><option value="Mensal">Mensal</option><option value="Anual">Anual</option></select></div></div><div className="grid grid-cols-2 gap-4"><div><label className="block text-xs font-medium text-gray-400 mb-1">Custo Un (R$)</label><input type="number" step="0.01" required value={editLicenseData ? editLicenseData.custo : newLicense.custo} onChange={(e) => editLicenseData ? setEditLicenseData({...editLicenseData, custo: e.target.value}) : setNewLicense({...newLicense, custo: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white focus:border-brandGreen outline-none" /></div><div><label className="block text-xs font-medium text-gray-400 mb-1">Qtd Comprada</label><input type="number" required min={editLicenseData ? editLicenseData.quantidade_em_uso : 1} value={editLicenseData ? editLicenseData.quantidade_total : newLicense.quantidade_total} onChange={(e) => editLicenseData ? setEditLicenseData({...editLicenseData, quantidade_total: e.target.value}) : setNewLicense({...newLicense, quantidade_total: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white focus:border-brandGreen outline-none" /></div></div><div className="mt-2"><label className="block text-xs font-medium text-gray-400 mb-1">Data de Renovação / Vencimento</label><input type="date" value={editLicenseData ? editLicenseData.data_renovacao : newLicense.data_renovacao} onChange={(e) => editLicenseData ? setEditLicenseData({...editLicenseData, data_renovacao: e.target.value}) : setNewLicense({...newLicense, data_renovacao: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white focus:border-brandGreen outline-none" /></div><button type="submit" className="w-full bg-brandGreen text-white py-4 rounded-full font-bold mt-4">{editLicenseData ? 'Salvar Alterações' : 'Criar Licença'}</button></form></div></div>)}

      {viewAssetDetails && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-md shadow-2xl relative overflow-y-auto max-h-[90vh]"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white">{isEditingDetails ? 'Editar Ativo' : 'Detalhes do Ativo'}</h2><div className="flex gap-2">{!isEditingDetails && (<button onClick={() => openEditDetails(viewAssetDetails)} className="text-brandGreen hover:text-brandGreenHover bg-brandGreen/10 p-2 rounded-lg flex items-center gap-2 text-sm"><Edit2 className="w-4 h-4"/> Editar</button>)}<button onClick={() => { setViewAssetDetails(null); setIsEditingDetails(false); }} className="text-gray-400 hover:text-white p-2"><X className="w-6 h-6" /></button></div></div>{!isEditingDetails ? (<div className="space-y-4">{viewAssetDetails.asset_type === 'Starlink' && viewAssetDetails.starlink && (<><div className="bg-black/50 p-4 rounded-xl border border-gray-800"><p className="text-sm text-brandGreen font-semibold mb-2 flex items-center gap-2"><Briefcase className="w-4 h-4"/> Local</p><p className="text-gray-300"><strong className="text-gray-500">Projeto:</strong> {viewAssetDetails.starlink.projeto}</p></div></>)}{viewAssetDetails.asset_type === 'Notebook' && viewAssetDetails.notebook && (<div className="bg-black/50 p-4 rounded-xl border border-gray-800 space-y-2"><p className="text-gray-300"><strong className="text-gray-500">Modelo:</strong> {viewAssetDetails.notebook.modelo}</p><p className="text-gray-300"><strong className="text-gray-500">S/N:</strong> {viewAssetDetails.notebook.serial_number}</p><p className="text-gray-300"><strong className="text-gray-500">Patrimônio:</strong> {viewAssetDetails.notebook.patrimonio}</p></div>)}</div>) : (<form onSubmit={saveEditDetails} className="flex flex-col gap-3">{viewAssetDetails.asset_type === 'Notebook' && (<><input type="text" placeholder="Patrimônio" value={editFormData.patrimonio} onChange={e => setEditFormData({...editFormData, patrimonio: e.target.value})} className="bg-black border border-gray-700 rounded-lg p-2 text-white" /><input type="text" placeholder="Serial Number" value={editFormData.serial_number} onChange={e => setEditFormData({...editFormData, serial_number: e.target.value})} className="bg-black border border-gray-700 rounded-lg p-2 text-white" /><input type="text" placeholder="Modelo" value={editFormData.modelo_notebook} onChange={e => setEditFormData({...editFormData, modelo_notebook: e.target.value})} className="bg-black border border-gray-700 rounded-lg p-2 text-white" /></>)}<div className="flex gap-2 mt-4"><button type="button" onClick={() => setIsEditingDetails(false)} className="flex-1 bg-gray-800 text-gray-300 py-3 rounded-lg font-bold">Cancelar</button><button type="submit" className="flex-1 bg-brandGreen text-white py-3 rounded-lg font-bold flex items-center justify-center gap-2"><Save className="w-4 h-4"/> Salvar</button></div></form>)}</div></div>)}

      {isAssignEmployeeModalOpen && activeEmployee && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-md shadow-2xl"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white">Atribuir a {activeEmployee.nome.split(' ')[0]}</h2><button onClick={() => setIsAssignEmployeeModalOpen(false)} className="text-gray-400 hover:text-brandGreen"><X className="w-6 h-6" /></button></div><form onSubmit={submitAssignment} className="flex flex-col gap-4"><select required value={selectedItemForAssign} onChange={(e) => setSelectedItemForAssign(e.target.value)} className="w-full bg-black border border-brandGreen/50 rounded-xl p-3 text-white focus:border-brandGreen outline-none"><option value="" disabled>Escolha na lista...</option>{availableAssignables.length === 0 && <option disabled>Estoque vazio!</option>}{availableAssignables.map(asset => (<option key={asset.id} value={asset.id}>{asset.asset_type}: {asset.notebook?.patrimonio || asset.celular?.imei || asset.chip?.numero}</option>))}</select><button type="submit" disabled={availableAssignables.length === 0} className="w-full bg-brandGreen text-white py-4 rounded-full font-bold mt-4 shadow-lg">Confirmar Atribuição</button></form></div></div>)}

      {isAssignAssetModalOpen && activeAsset && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-md shadow-2xl"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white">Atribuir {activeAsset.notebook?.patrimonio || activeAsset.asset_type}</h2><button onClick={() => setIsAssignAssetModalOpen(false)} className="text-gray-400 hover:text-brandGreen"><X className="w-6 h-6" /></button></div><form onSubmit={submitAssignment} className="flex flex-col gap-4"><select required value={selectedItemForAssign} onChange={(e) => setSelectedItemForAssign(e.target.value)} className="w-full bg-black border border-brandGreen/50 rounded-xl p-3 text-white outline-none"><option value="" disabled>Escolha na lista...</option>{employees.filter(e => e.status !== 'Desligado').map(emp => (<option key={emp.id} value={emp.id}>{emp.nome}</option>))}</select><button type="submit" className="w-full bg-brandGreen text-white py-4 rounded-full font-bold mt-4">Confirmar Atribuição</button></form></div></div>)}

      {isHistoryModalOpen && activeAsset && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-2xl shadow-2xl relative"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white flex items-center gap-2"><Clock className="text-purple-400"/> Histórico de Donos</h2><button onClick={() => setIsHistoryModalOpen(false)} className="text-gray-400 hover:text-white p-2"><X className="w-6 h-6" /></button></div><div className="max-h-96 overflow-y-auto pr-2">{(!activeAsset.assignments || activeAsset.assignments.length === 0) ? (<p className="text-gray-500 text-center py-4">Este equipamento nunca foi atribuído.</p>) : (<div className="space-y-4">{activeAsset.assignments.sort((a,b) => new Date(b.assigned_at) - new Date(a.assigned_at)).map((assignment, idx) => (<div key={idx} className="bg-black border border-gray-800 p-4 rounded-xl flex justify-between items-center"><div><p className="text-brandGreen font-bold">{assignment.employee?.nome || 'Desconhecido'}</p></div><div className="text-right text-sm"><p className="text-gray-300"><strong>Início:</strong> {new Date(assignment.assigned_at).toLocaleDateString('pt-BR')}</p><p className={assignment.returned_at ? 'text-gray-300' : 'text-blue-400 font-bold'}><strong>Fim:</strong> {assignment.returned_at ? new Date(assignment.returned_at).toLocaleDateString('pt-BR') : 'Em uso'}</p></div></div>))}</div>)}</div></div></div>)}

      {isMaintenanceModalOpen && activeAsset && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-md shadow-2xl"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white">Enviar para Manutenção</h2><button onClick={() => setIsMaintenanceModalOpen(false)} className="text-gray-400 hover:text-yellow-400"><X className="w-6 h-6" /></button></div><form onSubmit={submitMaintenance} className="flex flex-col gap-4"><input type="text" required value={maintenanceForm.chamado} onChange={(e) => setMaintenanceForm({...maintenanceForm, chamado: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white outline-none" placeholder="Ex: INC-10293" /><textarea required value={maintenanceForm.observacao} onChange={(e) => setMaintenanceForm({...maintenanceForm, observacao: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white outline-none h-24 resize-none" placeholder="Ex: Tela piscando..." /><button type="submit" className="w-full bg-yellow-500 hover:bg-yellow-400 text-black py-4 rounded-full font-bold mt-4 shadow-lg transition-colors">Registrar Manutenção</button></form></div></div>)}

      {isEditMaintenanceModalOpen && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-md shadow-2xl"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white flex items-center gap-2"><Edit2 className="w-5 h-5 text-blue-400"/> Atualizar Tratativa</h2><button onClick={() => setIsEditMaintenanceModalOpen(false)} className="text-gray-400 hover:text-blue-400"><X className="w-6 h-6" /></button></div><form onSubmit={submitEditMaintenance} className="flex flex-col gap-4"><input type="text" required value={editMaintenanceForm.chamado} onChange={(e) => setEditMaintenanceForm({...editMaintenanceForm, chamado: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white outline-none" placeholder="Ex: INC-10293" /><textarea required value={editMaintenanceForm.observacao} onChange={(e) => setEditMaintenanceForm({...editMaintenanceForm, observacao: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white outline-none h-24 resize-none" placeholder="Atualize o status do conserto..." /><button type="submit" className="w-full bg-blue-600 hover:bg-blue-500 text-white py-4 rounded-full font-bold mt-4 shadow-lg transition-colors flex items-center justify-center gap-2"><Save className="w-5 h-5"/> Salvar Histórico</button></form></div></div>)}

      {isEmployeeModalOpen && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-md shadow-2xl"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white">Cadastrar Colaborador</h2><button onClick={() => setIsEmployeeModalOpen(false)} className="text-gray-400 hover:text-brandGreen"><X className="w-6 h-6" /></button></div><form onSubmit={handleCreateEmployee} className="flex flex-col gap-4"><input type="text" placeholder="Nome Completo" required value={newEmployee.nome} onChange={(e) => setNewEmployee({...newEmployee, nome: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white outline-none" /><input type="email" placeholder="E-mail" required value={newEmployee.email} onChange={(e) => setNewEmployee({...newEmployee, email: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white outline-none" /><input type="text" placeholder="Departamento" required value={newEmployee.departamento} onChange={(e) => setNewEmployee({...newEmployee, departamento: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white outline-none" /><button type="submit" className="w-full bg-brandGreen text-white py-4 rounded-full font-bold mt-4">Salvar Colaborador</button></form></div></div>)}

      {isAssetModalOpen && (<div className="fixed inset-0 bg-black/80 flex items-center justify-center p-4 backdrop-blur-sm z-50 overflow-y-auto"><div className="bg-gray-900 border border-gray-800 rounded-3xl p-6 w-full max-w-2xl shadow-2xl my-8"><div className="flex justify-between items-center mb-6 border-b border-gray-800 pb-4"><h2 className="text-xl font-bold text-white">Cadastrar Novo Ativo no Estoque</h2><button onClick={() => setIsAssetModalOpen(false)} className="text-gray-400 hover:text-brandGreen"><X className="w-6 h-6" /></button></div><form onSubmit={handleCreateAsset} className="flex flex-col gap-4"><div className="grid grid-cols-2 gap-4"><div><label className="block text-sm font-medium text-gray-400 mb-1">Tipo de Equipamento</label><select value={newAsset.asset_type} onChange={(e) => setNewAsset({...newAsset, asset_type: e.target.value})} className="w-full bg-black border border-gray-700 rounded-xl p-3 text-white outline-none"><option value="Notebook">Notebook</option><option value="Celular">Aparelho Celular</option><option value="CHIP">Linha Telefônica / CHIP</option><option value="Starlink">Antena Starlink</option></select></div><div><label className="block text-sm font-medium text-gray-400 mb-1">Status Inicial</label><input type="text" disabled value="Disponível (Estoque)" className="w-full bg-gray-800 border border-gray-700 rounded-xl p-3 text-gray-400 outline-none cursor-not-allowed" /></div></div>{newAsset.asset_type === 'Notebook' && (<><div className="grid grid-cols-2 gap-4"><input type="text" placeholder="Serial Number" required value={newAsset.serial_number} onChange={(e) => setNewAsset({...newAsset, serial_number: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white outline-none" /><input type="text" placeholder="Patrimônio" required value={newAsset.patrimonio} onChange={(e) => setNewAsset({...newAsset, patrimonio: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white outline-none" /></div><div className="grid grid-cols-3 gap-4"><input type="text" placeholder="Modelo" required value={newAsset.modelo_notebook} onChange={(e) => setNewAsset({...newAsset, modelo_notebook: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white outline-none" /><input type="date" required value={newAsset.garantia} onChange={(e) => setNewAsset({...newAsset, garantia: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white outline-none text-sm" /><select value={newAsset.status_garantia} onChange={(e) => setNewAsset({...newAsset, status_garantia: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white outline-none"><option value="No prazo">No prazo</option><option value="Vencido">Vencido</option></select></div></>)}{newAsset.asset_type === 'Starlink' && (<><div className="grid grid-cols-2 gap-4"><input type="text" placeholder="Modelo" required value={newAsset.modelo_starlink} onChange={(e) => setNewAsset({...newAsset, modelo_starlink: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /><input type="text" placeholder="Projeto" required value={newAsset.projeto} onChange={(e) => setNewAsset({...newAsset, projeto: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /></div><div className="grid grid-cols-2 gap-4"><input type="text" placeholder="Localização" required value={newAsset.localizacao} onChange={(e) => setNewAsset({...newAsset, localizacao: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /><input type="text" placeholder="Responsável do Local" required value={newAsset.responsavel} onChange={(e) => setNewAsset({...newAsset, responsavel: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /></div><div className="grid grid-cols-3 gap-4"><input type="email" placeholder="E-mail" required value={newAsset.email} onChange={(e) => setNewAsset({...newAsset, email: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /><input type="text" placeholder="Senha Conta" required value={newAsset.senha} onChange={(e) => setNewAsset({...newAsset, senha: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /><input type="text" placeholder="Senha Wi-Fi (Opcional)" value={newAsset.senha_roteador} onChange={(e) => setNewAsset({...newAsset, senha_roteador: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /></div></>)}{newAsset.asset_type === 'Celular' && (<div className="grid grid-cols-2 gap-4"><input type="text" placeholder="Modelo do Aparelho" required value={newAsset.modelo_celular} onChange={(e) => setNewAsset({...newAsset, modelo_celular: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /><input type="text" placeholder="IMEI" required maxLength={15} value={newAsset.imei} onChange={(e) => setNewAsset({...newAsset, imei: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /></div>)}{newAsset.asset_type === 'CHIP' && (<div className="grid grid-cols-2 gap-4"><input type="text" placeholder="ICCID" required maxLength={20} value={newAsset.iccid} onChange={(e) => setNewAsset({...newAsset, iccid: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /><input type="text" placeholder="Número (Linha)" required value={newAsset.numero} onChange={(e) => setNewAsset({...newAsset, numero: e.target.value})} className="bg-black border border-gray-700 rounded-xl p-3 text-white" /></div>)}<button type="submit" className="w-full bg-brandGreen text-white py-4 rounded-full font-bold mt-6">Adicionar ao Estoque</button></form></div></div>)}
    </div>
  );
}

export default App;